import { FC, ReactElement } from 'react';
import { Card } from '../../components';

const BuildsAndTests: FC = (): ReactElement => {
  return (
    <Card title="Builds and Tests" className="p-5 gap-2">
      Testing
    </Card>
  );
};

export default BuildsAndTests;
